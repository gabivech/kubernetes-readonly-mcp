# Obrigatório: nomes exatos de namespaces, separados por vírgula. Sem curingas.
K8S_ALLOWED_NAMESPACES=default

# Opcional. Quando ausente, o cliente usa a configuração padrão do kubectl.
KUBECONFIG=
